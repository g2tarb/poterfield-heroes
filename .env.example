# === Database ===
DATABASE_URL=postgresql://ph:ph_dev@localhost:5432/porterfield

# === Redis ===
REDIS_URL=redis://localhost:6379

# === API ===
API_PORT=3031
API_HOST=0.0.0.0
NODE_ENV=development
CORS_ORIGIN=http://localhost:3030
NEXT_PUBLIC_API_URL=http://localhost:3031

# === Auth (mono-user) ===
SESSION_SECRET=change_me_32_chars_minimum_xxxxxxx
ACCESS_PASSWORD=change_me_personal_password

# === IA Providers ===
ANTHROPIC_API_KEY=sk-ant-xxx
VOYAGE_API_KEY=pa-xxx
# ELEVENLABS_API_KEY : reportée Sprint 9 (TTS coach)

# === Budget cap (cents per month) ===
MONTHLY_AI_BUDGET_CENTS=5000

# === GitHub Integration ===
GITHUB_APP_ID=
GITHUB_APP_PRIVATE_KEY=
GITHUB_WEBHOOK_SECRET=
GITHUB_INSTALLATION_ID=

# === Sentry (optionnel) ===
# Laisser vide pour désactiver. Si posées, l'API + le web envoient leurs
# exceptions à Sentry (tracesSampleRate=0.1).
SENTRY_DSN=
NEXT_PUBLIC_SENTRY_DSN=
